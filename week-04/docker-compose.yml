version: '3.8'

services:
  wiki:
    image: wiki:local
    ports:
      - "8080:80"
    networks:
      - frontend

  db:
    image: db:local
    networks:
      - backend

networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true
